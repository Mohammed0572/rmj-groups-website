# Server Configuration
PORT=5000

# MongoDB Configuration
# Replace the placeholder connection string with your actual MongoDB URI
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/rmjgroups?retryWrites=true&w=majority

# Authentication Secret
# Replace with a strong, random string in production
JWT_SECRET=your_super_secret_jwt_key_here
